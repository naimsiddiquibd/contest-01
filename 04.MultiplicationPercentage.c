#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    int X, Y, Z;
    scanf("%d%%",&X);
    scanf("%d%%",&Y);
    Z = X * Y;
    printf("%d%c",Z,37);
    return 0;
}
