#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    int N, M, Total;
    scanf("%d%d",&N,&M);
    Total = N + M;
    printf("%d",Total);
    return 0;
}
